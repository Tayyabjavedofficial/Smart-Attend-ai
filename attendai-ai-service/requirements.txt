# Core runtime
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.17
numpy==2.1.2

# Optional real face-detection backend (uncomment to enable).
# Without these the stub backend is used - the API contract is identical so
# the rest of the stack works unchanged.
#   opencv-python-headless==4.10.0.84
#   Pillow==11.0.0

# Dev / test
pytest==8.3.3
httpx==0.27.2
